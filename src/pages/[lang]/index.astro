---
import BaseLayout from "../../layouts/BaseLayout.astro";
import SectionHeading from "../../components/SectionHeading.astro";
import CourseCard from "../../components/CourseCard.astro";
import TestimonialSlider from "../../components/TestimonialSlider.astro";
import {
  getCourseWithMedia,
  getTranslation,
  isLocale,
  localePath,
  locales,
  siteAssets,
  type Locale,
} from "../../i18n";

export function getStaticPaths() {
  return locales.map((lang) => ({ params: { lang } }));
}

const lang = Astro.params.lang!;
if (!isLocale(lang)) return Astro.redirect("/lt/");

const locale = lang as Locale;
const t = getTranslation(locale);
const courses = t.courses.map(getCourseWithMedia);
---

<BaseLayout locale={locale} t={t} title={t.site.name}>
  <section class="relative overflow-hidden border-b border-border">
    <div
      class="pointer-events-none absolute inset-0 opacity-40"
      aria-hidden="true"
      style="background-image: radial-gradient(circle at 20% 20%, rgba(91,75,183,0.15), transparent 40%), radial-gradient(circle at 80% 0%, rgba(201,162,39,0.12), transparent 35%);"
    />
    <div class="mx-auto grid max-w-6xl items-center gap-8 px-4 py-12 sm:px-5 sm:py-16 md:grid-cols-2 md:gap-10 md:py-24">
      <div class="animate-fade-up order-2 md:order-1">
        <p class="mb-4 inline-flex items-center gap-2 rounded-full border border-accent/20 bg-accent/5 px-4 py-1.5 text-sm font-medium text-accent">
          {t.home.heroEyebrow}
        </p>
        <h1 class="font-display text-3xl font-semibold leading-tight tracking-tight text-ink sm:text-4xl md:text-5xl lg:text-6xl">
          {t.site.tagline}
        </h1>
        <p class="mt-6 max-w-xl text-lg leading-relaxed text-ink-muted">{t.site.description}</p>
        <p class="mt-3 text-sm text-ink-muted">
          {t.site.author}
        </p>
        <div class="mt-8 flex flex-wrap gap-4">
          <a
            href="#kursai"
            class="inline-flex items-center rounded-full bg-accent px-6 py-3 text-sm font-semibold text-white shadow-lg shadow-accent/25 transition hover:bg-accent-light"
          >
            {t.home.ctaCourses}
          </a>
          <a
            href={localePath(locale, "author-and-instructors")}
            class="inline-flex items-center rounded-full border border-border bg-surface-elevated px-6 py-3 text-sm font-semibold text-ink transition hover:border-accent hover:text-accent"
          >
            {t.home.ctaTeam}
          </a>
        </div>
      </div>

      <div class="animate-fade-up animate-fade-up-delay-2 relative order-1 mx-auto w-full max-w-[11rem] sm:max-w-xs md:order-2 md:max-w-md">
        <div class="absolute -inset-3 rounded-3xl bg-gradient-to-br from-accent/20 to-gold/20 blur-2xl md:-inset-4 md:rounded-[2rem]" aria-hidden="true" />
        <img
          src={siteAssets.logo}
          alt={t.common.logoAlt}
          width="550"
          height="550"
          class="relative rounded-3xl border border-border bg-surface-elevated p-4 shadow-lg sm:p-6 md:rounded-[2rem] md:p-8 md:shadow-xl"
        />
      </div>
    </div>
  </section>

  <section id="apie" class="mx-auto max-w-6xl px-4 py-14 sm:px-5 sm:py-20 md:py-28 scroll-mt-24">
    <SectionHeading
      eyebrow={t.home.aboutEyebrow}
      title={t.home.aboutTitle}
      description={t.home.aboutDescription}
    />
    <div class="grid gap-4 sm:gap-6 md:grid-cols-3">
      {t.intro.map((paragraph) => (
        <p class="rounded-2xl border border-border bg-surface-elevated p-4 text-sm leading-relaxed text-ink-muted shadow-md ring-1 ring-black/[0.03] sm:p-6 sm:text-base sm:shadow-sm sm:ring-0">
          {paragraph}
        </p>
      ))}
    </div>
  </section>

  <section id="kursai" class="border-y border-border bg-white/50 py-14 sm:py-20 md:py-28 scroll-mt-24">
    <div class="mx-auto max-w-6xl px-4 sm:px-5">
      <SectionHeading
        eyebrow={t.home.coursesEyebrow}
        title={t.home.coursesTitle}
        description={t.home.coursesDescription}
        align="center"
      />
      <div class="flex flex-col gap-5 sm:gap-8 md:space-y-10 md:gap-0">
        {courses.map((course, index) => (
          <CourseCard course={course} labels={t.common} index={index} />
        ))}
      </div>
    </div>
  </section>

  <section id="atsiliepimai" class="mx-auto max-w-4xl px-4 py-14 sm:px-5 sm:py-20 md:py-28 scroll-mt-24">
    <SectionHeading
      eyebrow={t.home.testimonialsEyebrow}
      title={t.home.testimonialsTitle}
      description={t.home.testimonialsDescription}
      align="center"
    />
    <TestimonialSlider items={t.testimonials} labels={t.common} />
  </section>

  <section class="border-t border-border bg-ink py-16 text-white md:py-20">
    <div class="mx-auto max-w-3xl px-5 text-center">
      <h2 class="font-display text-3xl font-semibold md:text-4xl">{t.home.ctaTitle}</h2>
      <p class="mt-4 text-white/70">{t.home.ctaDescription}</p>
      <a
        href={localePath(locale, "author-and-instructors")}
        class="mt-8 inline-flex items-center rounded-full bg-gold px-8 py-3 text-sm font-semibold text-ink transition hover:bg-gold-light"
      >
        {t.home.ctaButton}
      </a>
    </div>
  </section>
</BaseLayout>
